env = {
    "+": lambda x, y: x+y,
    "-": lambda x, y: x-y,
    "/": lambda x, y: x/y,
    "*": lambda x, y: x*y,
    "&": lambda x, y: x&y,
    "|": lambda x, y: x|y,
    "^": lambda x, y: x^y,
    "max": lambda x, y: max(x, y),
    "min": lambda x, y: min(x, y),
}

